/* 1. The number of elements in the tuple is fixed.
   2. The types of elements are known, and need not be the same
*/

let numbers: [string, number, number?]; // the last one is optional

numbers = ["One hundred", 200]
